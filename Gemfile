source 'https://rubygems.org'

gem 'rails', '4.2.1'
gem 'pg'

gem 'tweetstream'

# This isn't actually needed since we aren't running a web worker
gem 'puma'

group :development, :test do
  gem 'dotenv-rails'
  gem 'pry'
end

